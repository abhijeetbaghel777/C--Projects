#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n;
    printf("Enter size:");
    scanf("%d",&n);
    int *arr = (int *)calloc(n,sizeof(int));

    printf("Enter %d elements:\n",n);
    for
    
}